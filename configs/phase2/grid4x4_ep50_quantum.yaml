# Same as grid4x4_quantum.yaml but 50 epochs: val loss was still falling at
# epoch 30. Both arms must use identical epochs, so control gets 50 too.
extends: grid4x4_quantum.yaml
epochs: 50
seeds: [0]
